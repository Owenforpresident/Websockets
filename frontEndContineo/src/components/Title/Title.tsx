import Typography from '@mui/material/Typography';
import './Title.styles.css';

const Title = () => {
  return (
    <Typography variant="h5" className="title">Real-time Leaderboard</Typography>
  );
};

export default Title;
